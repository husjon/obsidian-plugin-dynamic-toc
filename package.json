{
  "name": "obsidian-plugin-dynamic-toc",
  "version": "0.0.27",
  "description": "An Obsidian plugin to generate Tables of Contents that stay up to date with your document outline.",
  "main": "main.js",
  "license": "MIT",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "prebuild": "yarn test",
    "type-check": "tsc --noEmit",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "_need_replacements__release:dry": "standard-version -t '' --dry-run",
    "_need_replacements__release": "standard-version -t '' -a",
    "version": "node version-bump.mjs && git add manifest.json versions.json",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "@types/node": "^16.11.6",
    "@typescript-eslint/eslint-plugin": "^5.29.0",
    "@typescript-eslint/parser": "^5.29.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.19.5",
    "jest": "^27.4.5",
    "obsidian": "latest",
    "prettier": "^3.1.0",
    "ts-jest": "^27.1.2",
    "tslib": "^2.6.2",
    "typescript": "^4.5.4"
  }
}
